# Copy to `.env` in this directory (same folder as `docker-compose.yml`).
# Docker Compose loads `.env` automatically. Do not commit `.env`.

# Cloudflare Tunnel token for the `cloudflared` service.
# Zero Trust → Networks → Tunnels → your tunnel → Install connector → copy token after `--token`.
TUNNEL_TOKEN=token
